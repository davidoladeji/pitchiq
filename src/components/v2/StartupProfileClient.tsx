"use client";

// Placeholder — startup profile v2 will use the mockup design.
// The classic startup-profile page handles its own v2 shell wrapping
// via useDashboardVersion() hook directly.
export default function StartupProfileV2() {
  return null;
}
